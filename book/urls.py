from django.urls import path
from . import views

# urlpatterns = [
#     path('livros/', views.ListBooksView.as_view(), name='books'),
#     path('cadastrar-livro/', views.CreateBook.as_view(), name='create_book'),
# ]